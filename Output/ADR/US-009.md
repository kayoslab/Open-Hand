No ADR required.

Rationale: US-009 adds tier-specific background tints and pill-shaped styling to the existing `.tierMarker` element in CardVisual, plus new background color tokens in `tokens.css`. This is a contained CSS enhancement within the design token system and component styling approach already established by ADR-004. No new dependencies, architectural patterns, module boundaries, or routing changes are introduced. The plan works entirely within existing conventions — CSS Modules for scoped styles, CSS custom properties for tokens, and the existing tier class structure (`.tierOpen`, `.tierWorking`, `.tierDeep`). Both existing ADRs (shell layout, hash router) are unaffected.

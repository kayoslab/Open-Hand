No ADR required.

Rationale: US-011 adds category filtering through a pure domain function `filterByCategories` in `src/domain/filterCards.ts`, a `CategoryFilter` UI component in `src/ui/CategoryFilter/`, and state wiring in the existing `BrowseAllCards.tsx`. This is a contained feature addition within established conventions — pure domain logic in `src/domain/`, shared UI components in `src/ui/`, CSS Modules for scoped styling per ADR-004, and design tokens in `tokens.css`. No new dependencies, architectural patterns, module boundaries, or routing changes are introduced. Both existing ADRs (ADR-004 shell layout, ADR-017 hash router) are unaffected. The `Category` union type already exists in `src/domain/card.ts` with the exact four values the plan targets, so there is no risk of type/UI mismatch. Note: US-012 (tier filters) also plans to use `src/domain/filterCards.ts` — whichever ticket lands first creates the file; the second adds to it. This is standard incremental development within the same module and requires no coordination beyond normal merge practices.

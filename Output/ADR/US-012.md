No ADR required.

Rationale: US-012 adds tier filtering through a pure domain function `filterByTier` in `src/domain/filterCards.ts`, a `TierFilter` UI component in `src/ui/TierFilter/`, and state wiring in the existing `BrowseAllCards.tsx`. All additions follow established conventions — pure domain logic in `src/domain/`, shared UI components in `src/ui/`, CSS Modules for scoped styling per ADR-004, and tier color tokens already defined in `tokens.css`. No new dependencies, architectural patterns, module boundaries, or routing changes are introduced. The plan is a verification pass over an already-implemented feature, confirming correctness through existing test suites and lint/typecheck. Both existing ADRs (ADR-004 shell layout, ADR-017 hash router) are unaffected. Note: `filterByCategories` is exported from `src/domain/index.ts` but belongs to US-011 — the plan correctly identifies this as a separate concern to avoid accidentally invoking.

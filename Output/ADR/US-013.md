No ADR required.

Rationale: US-013 adds text search through a pure domain function `filterByText` in `src/domain/filterCards.ts`, a `SearchInput` UI component in `src/ui/SearchInput/`, and state wiring in the existing `BrowseAllCards.tsx`. This follows the identical pattern established by US-011 (category filtering) and US-012 (tier filtering) — pure domain logic in `src/domain/`, shared UI components in `src/ui/`, CSS Modules for scoped styling per ADR-004, and filter composition in `BrowseAllCards.tsx`. No new dependencies, architectural patterns, module boundaries, or routing changes are introduced. The plan correctly composes `filterByText` with the existing `filterByTier` pipeline, which is standard functional filter chaining within the established domain layer. Both existing ADRs (ADR-004 shell layout, ADR-017 hash router) are unaffected. The risk about field naming (`prompt`/`guidance` vs `title`/`question`) is a correctness concern, not an architectural one — the plan correctly identifies it and the field mapping can be verified during implementation.

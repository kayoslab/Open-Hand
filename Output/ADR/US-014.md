No ADR required, but the plan MUST be revised before approval.

Rationale: The pure draw logic in `src/domain/drawRandom.ts`, the SingleDraw component in `src/features/play/`, CSS Module styling, unit tests, and component tests are all contained additions within existing conventions (domain logic in `src/domain/`, CSS Modules per ADR-004, feature folder structure per CLAUDE.md). No new architectural patterns, dependencies, or module boundaries are introduced for those steps.

However, **step 5 and the risks section conflict with ADR-017** (Client-Side Hash Router Introduction), which established a working custom hash-based router in App.tsx. The plan states "No routing exists yet — App.tsx is a static shell" and proposes creating "a minimal hash-based page switcher in App.tsx" — but ADR-017 already implemented exactly this. The `#/play` hash route is already referenced in Nav.tsx links.

The plan must be corrected to:
1. Remove the proposal to "create a minimal hash-based page switcher" — the hash router already exists per ADR-017
2. Simply register the `#/play` route in App.tsx's existing route resolution switch, mapping it to the SingleDraw component — following the same pattern used by the Guide page
3. Remove the risk "No routing exists yet" — it is factually incorrect given ADR-017
4. Remove the risk about US-014 being "the first ticket to introduce page navigation" — US-017 already introduced it

Once step 5 is aligned with ADR-017's existing hash router, no new ADR is needed — the plan works entirely within established conventions.

No ADR required.

Rationale: US-016 adds pairing references through a pure domain function `findCardByNumber` in `src/domain/findCard.ts`, optional props (`pairedCard`, `onPairClick`) on the existing `CardVisual` component, CSS additions within the existing CardVisual CSS Module, and state wiring in `BrowseAllCards.tsx` and play mode components. All additions follow established conventions — pure domain logic in `src/domain/`, CSS Modules per ADR-004, optional prop extensions that don't break existing call sites, and hash-based navigation per ADR-017 for cross-feature linking (play → browse). No new dependencies, architectural patterns, module boundaries, or routing capabilities are introduced.

The plan's risk about `pairsWith` potentially containing multiple values (e.g., "42, 43, 44") is unfounded — CSV inspection confirms all `pairsWith` values are single numbers, matching the existing `pairsWith?: number` type in `card.ts`. No parser changes are needed.

The cross-feature navigation in step 6 (play mode `onPairClick` navigating to `#/browse` and scrolling to a card) uses the existing hash router — this is a standard `window.location.hash` assignment, not a new routing pattern. The scroll-to-card behavior via `data-card-number` attributes and `document.querySelector` is a contained DOM interaction pattern that introduces no architectural coupling. The risk about filtered cards being invisible after scroll is a UX concern, not an architectural one — the suggested mitigation (clear filters or show a toast) stays within existing component state management. Both existing ADRs (ADR-004 shell layout, ADR-017 hash router) are unaffected.

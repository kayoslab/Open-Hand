No ADR required.

Rationale: US-018 adds a preferences persistence module in `src/domain/preferences.ts` using the browser's localStorage API, with integration points in App.tsx (route restoration via the existing hash router) and BrowseAllCards.tsx (tier filter and search query restoration). All additions follow established conventions — domain logic in `src/domain/`, the existing hash router pattern per ADR-017 for route persistence, and existing component state patterns in BrowseAllCards.tsx for filter persistence. No new dependencies, architectural patterns, module boundaries, or routing capabilities are introduced. The localStorage access is wrapped in try/catch with silent failure, satisfying the "no backend required" constraint already established in CLAUDE.md. The type guard validation pattern for stored data is a standard defensive coding practice, not a new architectural pattern. Both existing ADRs (ADR-004 shell layout, ADR-017 hash router) are unaffected. The plan correctly uses `window.location.hash` assignment for route restoration rather than introducing any new navigation mechanism, and keeps all persistence logic in a single module with a clean API surface (`loadPreferences`, `savePreferences`, `clearPreferences`) that other features can adopt without architectural changes.

No ADR required.

Rationale: US-019 adds accessibility improvements across existing components — skip-navigation link in Layout.tsx, heading hierarchy fixes, :focus-visible normalization, ARIA attributes, color contrast adjustments in tokens.css, and aria-live regions for dynamic content. Every change is a contained enhancement to existing markup and CSS within established conventions: CSS custom properties in tokens.css per ADR-004, CSS Modules for scoped component styles, semantic HTML already in use, and no changes to routing (ADR-017 unaffected). No new dependencies, architectural patterns, module boundaries, or component structures are introduced. The skip-link and sr-only utility class in tokens.css are standard CSS patterns consistent with the existing token system. Adjusting --color-text-muted for contrast compliance is a token-level change within ADR-004's design token approach. The DrawThreeKeepOne nested-interactive refactor (step 6) is the highest-risk change but remains a component-internal markup fix with no API or architectural impact. All other ARIA additions are attribute-level changes to existing components with no structural consequences.

No ADR required.

Rationale: US-019 adds accessibility improvements across existing components — skip-navigation link in Layout.tsx, heading hierarchy fixes, :focus-visible normalization, ARIA attributes, color contrast adjustments in tokens.css, and disabled-state semantics. Every change is a contained enhancement to existing markup and CSS within established conventions: CSS custom properties and utility classes in tokens.css per ADR-004, CSS Modules for scoped component styles, semantic HTML already in use, and no changes to routing (ADR-017 unaffected). No new dependencies, architectural patterns, module boundaries, or component structures are introduced. The sr-only utility class in tokens.css is a standard CSS pattern consistent with the existing token system. Adjusting --color-text-muted and tier badge colors are token-level changes within ADR-004's design token approach. All ARIA additions are attribute-level changes to existing components with no structural impact.

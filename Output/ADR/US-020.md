No ADR required.\n\nRationale: US-020 is a deployment ticket that adds a `vercel.json` configuration file and connects the repository to Vercel's hosting platform. No application code, dependencies, architectural patterns, module boundaries, routing, or component structures are changed. The `vercel.json` SPA rewrite rule is a platform-level deployment configuration, not an application architecture decision — and is largely redundant given the existing hash-based routing (ADR-017), since hash fragments are never sent to the server and therefore cannot 404 on refresh. The build command (`npm run build`), output directory (`dist`), and Vite framework preset are all standard Vite conventions already implied by the tech stack in CLAUDE.md. Both existing ADRs (ADR-004 shell layout, ADR-017 hash router) are unaffected. No new dependencies, patterns, or conventions are introduced that would constrain future development.
